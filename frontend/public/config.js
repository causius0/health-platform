// Deploy-time configuration — edit this file after building (or let the Pages
// workflow overwrite it) without rebuilding the SPA.
// Same-origin deployment (dev, docker-compose): leave empty, the SPA calls /api.
// GitHub Pages (static, cross-origin): point it at the public backend URL, e.g.
//   window.__API_BASE__ = 'https://your-tunnel.example.com/api'
window.__API_BASE__ = ''
